using System;

class Program
{
    static void Main()
    {
        Sum(10, 20);
        Sum(a1 : 10, a2 : 20);
        Sum(a2 : 20, a1 : 10);
    }
    static void Sum(int a1, int a2)
    {
        Console.WriteLine(a1 - a2);
    }    
}
